struct ip_addr {
    i32 ipv4; // dummy type
}

fn handle_request(mut stream: TcpStream) {
    // handle requests
}

fn get_domain(ip_addr ip) -> std::str {
    // return domain name for the ip address
}
